import React from "react";

import RefugeeForm from "./RefugeeForm";

export default {
  component: RefugeeForm,
  title: `Components/RefugeeForm`,
};

const Template = () => {
  return <RefugeeForm />;
};

export const Primary = Template.bind({});
