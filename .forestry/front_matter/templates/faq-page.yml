---
label: FAQ page
hide_body: false
display_field: title
fields:
- name: title
  type: text
  config:
    required: false
  label: Title
- name: slug
  type: text
  config:
    required: false
  label: Unique name (slug)
  description: Used for creating unique links to section
- name: questions
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: false
    label: Title
  - name: answer
    type: textarea
    default: ''
    config:
      required: false
      wysiwyg: true
      schema:
        format: html-blocks
    label: Answer
  - name: resources
    type: field_group_list
    fields:
    - name: title
      type: text
      config:
        required: false
      label: Title
    - name: subtitle
      type: text
      config:
        required: false
      label: Subtitle
    - name: url
      type: text
      config:
        required: false
      label: URL
    config:
      min: 
      max: 
      labelField: title
    label: Resources
  config:
    min: 
    max: 
    labelField: 
  label: Questions
- name: weight
  type: number
  label: Weight
  description: Higher weight resources go up in the list, smaller go down
  default: 0
  required: true
  config:
    min: 
    max: 
    step: 
