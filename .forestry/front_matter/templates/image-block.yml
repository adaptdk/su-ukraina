---
label: Image block
hide_body: true
display_field: title
fields:
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: title
  type: text
  config:
    required: false
  label: Title
